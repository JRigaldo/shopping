import React from "react"

const Select = props => {
  // const { optionsNumbers } = props
  return (
    <select name="item-number" id="itemNumber">
      {/* {this.props.map(function (option, index) {
        return (
          <option key={index + 1} value={index + 1}>
            {option}
          </option>
        )
      })} */}
    </select>
  )
}

export default Select
